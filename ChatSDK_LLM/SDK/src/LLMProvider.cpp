#include "../include/LLMProvider.h"

// LLMProvider is a pure abstract interface.
// Virtual destructor body is defined here since LLMProvider.h
// declares pure virtual methods but no explicit destructor.
// (Compilers need the key function in a translation unit.)
